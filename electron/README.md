Run SqueakJS on Electron
========================

SqueakJS can be run on the [Electron](https://electron.atom.io) platform using
the files in this directory.  Electron requires [NodeJS](https://nodejs.org)
and [NPM](https://npmjs.org)

```bash
npm install
npm start
```
